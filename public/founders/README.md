Drop founder headshots here, named to match the `photoSlug` in `app/founders/page.tsx`
(e.g. `shreyas-j.jpg`). Recommended: square image, at least 320x320px, JPG/PNG/WebP.

If no file exists for a founder, the page automatically falls back to their initials —
nothing needs to change in code when a photo is missing.
